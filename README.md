# Usage arch-install.sh

```shell
cd ~ && git clone https://github.com/abdullah-rgb/arch-install-sh
cd ~/arch-install-sh
chmod +x *.sh
./arch-install.sh
```

# Usage chroot.sh

```shell
./chroot.sh
```
